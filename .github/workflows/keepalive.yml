name: Keep GitHub Actions Alive

on:
  schedule:
    - cron: "0 0 1 * *" # Runs on the 1st of every month
  workflow_dispatch:

jobs:
  cronjob-based-workflow-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: gautamkrishnar/keepalive-workflow@v2
